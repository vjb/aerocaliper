# MISSION: OPERATION "AEROCALIPER FINAL GAUNTLET"
**Role:** Principal Enterprise AI Architect & DevSecOps Lead.
**Objective:** Finalize the "AeroCaliper v4.0" Hackathon submission. You will sanitize all personal identifiers, implement Phase 2.5 MCP Discovery, fix all known architectural bugs, author a supercharged Jupyter Notebook, and run a continuous integration testing loop until the system is 100% verified.

**WARNING: NO LAZY ACKNOWLEDGEMENTS.** You must physically execute bash commands, run the Python code, read the outputs, and iterate on failures. You will output the final terminal logs in your response to prove execution.

---

# PHASE 1: SANITIZATION & ARCHITECTURAL FIXES

Execute the following surgical code edits to make the platform portable and fix UI/RAG bugs.

**1. Sanitize Workspace & Project IDs:**
* **`target_agent.py`**: Replace the hardcoded `PHOENIX_SPACE_URL` (line ~10) with dynamic `.env` loading:
    `space_id = os.getenv("ARIZE_SPACE_ID", "")`
    `PHOENIX_SPACE_URL = f"https://app.phoenix.arize.com/s/{space_id}" if space_id else "https://app.phoenix.arize.com"`
* **`aerocaliper.py`**: In `StandardMCPClient.connect()`, dynamically construct the `base_url` using `ARIZE_SPACE_ID` and apply it to the `PHOENIX_COLLECTOR_ENDPOINT` and `StdioServerParameters` args (removing `vjbeltrani`).
* **`aerocaliper.py`**: Change `google.cloud.logging.Client(project="aerocaliper")` to use `os.getenv("GOOGLE_CLOUD_PROJECT")`.

**2. Fix the Vertex AI RAG Hallucination:**
* **`aerocaliper.py`**: In `search_agent_builder_policy()`, REMOVE the logic that attempts to use `storage.Client()` to download a GCS blob. Vertex AI already returns the text. Extract it safely by iterating over `response.results` -> `document.derived_struct_data.get("extractive_answers")` -> `content`. Return the joined snippets or raise a `RuntimeError("Datastore indexing in progress...")`.

**3. Fix the Empirical Backtester Dataset Leak:**
* **`aerocaliper.py`**: In `run_experiment_background()`, add logic to filter the `golden_dataset.csv` rows based on `self.target_use_case`. If `target_use_case == "finops"`, filter OUT rows containing "pii", "salary", "contractor", or "draft". If `"hr"`, keep ONLY those rows. Calculate the pass rate strictly on the filtered denominator.

**4. Fix the UI SSE Backtest Glitch:**
* **`static/index.html`**: Update `updateBacktestUI()` so it doesn't hardcode 100% metrics. It must render a `span` with `id="livePassRate"` showing `"Awaiting backtest..."`. Update the SSE switch statement `case 'backtest_metrics':` to target `document.getElementById('livePassRate')` and update its `textContent` dynamically.

**5. Add the UI Bonus Badge:**
* **`static/index.html`**: In the `<nav>` badges, inject `<span class="badge" style="background: rgba(139,92,246,0.15); color: var(--accent-violet); border-color: var(--accent-violet); box-shadow: 0 0 10px rgba(139,92,246,0.3); animation: pulse 2s infinite;">✨ Self-Improvement Loop Active</span>`.

**6. Architect Phase 2.5 MCP Discovery:**
* **`aerocaliper.py`**: In `execute_remediation()`, directly after emitting Phase 2's completion, add a `try/except` block that calls `self.mcp.session.call_tool("get-projects", arguments={})` and `"get-datasets"`. Emit logs indicating "MCP Environment Discovery" is executing.

---

# PHASE 2: THE SUPERCHARGED JUPYTER NOTEBOOK

Create a new file: `Vertex_RAG_and_Arize_Eval_Deep_Dive.ipynb`. Write raw, valid Jupyter Notebook JSON.

**Notebook Structure:**
* **Section 1 (Markdown):** Introduction to Decoupled Compliance.
* **Section 2 (Code):** Load `.env` (Project ID, Location, FinOps Datastore ID).
* **Section 3 (Markdown + Code):** Explain Vertex AI "Extractive Answers" vs standard chunking. Execute a `discoveryengine_v1.SearchRequest` for "FinOps Routing Policy Spot Instances Budget Tag".
* **Section 4 (Code):** Parse the response and print the exact matched string.
* **Section 5 (Markdown + Code):** The "Universal" Proof. Swap datastore IDs, query for HR Privacy restrictions, and print the HR snippet.
* **Section 6: Closing the Loop with Arize (NEW EXTRA):**
    * *Markdown:* Explain how we pass the dynamically extracted Vertex AI policy directly into the Arize Phoenix SDK to run an LLM-as-a-Judge evaluation on our traces.
    * *Code:* Import `from phoenix.evals import llm_classify`. Write a mock Python function demonstrating how the `retrieved_policy` (from Section 4) is used as the evaluation rubric against a dummy trace DataFrame, tying GCP and Arize together.

**Generate Executed Artifact:**
Using your local python environment, run the logic contained in the notebook using dummy/mock API responses if live credentials are not present in your sandbox. Save the output with the executed cell results (stdout) into a secondary file named `Vertex_RAG_and_Arize_Eval_Deep_Dive_EXECUTED.ipynb`.

---

# PHASE 3: CONTINUOUS ITERATIVE TESTING GAUNTLET

You will now enter a strict DevSecOps feedback loop. You must execute tests, read the terminal errors, fix the code, and re-execute until perfect.

**Step 3.1: Backend Test Suite**
1. Fix the broken mock in `tests/test_backend.py`. Change `with patch("aerocaliper.mcp")` to `with patch("aerocaliper.StandardMCPClient.get_failed_spans", new_callable=AsyncMock)`.
2. Run `pytest tests/`. 
3. *Feedback Loop:* If ANY test fails, read the traceback, modify the python files to fix the error, and run `pytest tests/` again. Do not proceed until it is 100% green.

**Step 3.2: E2E Pipeline Validation**
1. Run `python scratch.py`.
2. *Feedback Loop:* Read the stdout. Verify that Phase 2.5 (MCP Discovery) prints in the logs. Verify that the RAG phase doesn't crash. Verify that the Empirical Backtester prints a pass rate based on the filtered dataset. If it crashes, fix the code and run again.

# FINAL OUTPUT REQUIREMENTS
1. Confirm all Phase 1 file modifications are complete.
2. Confirm both `.ipynb` files are saved to the disk.
3. Paste the FINAL, SUCCESSFUL terminal output from `pytest tests/` in a code block.
4. Paste the FINAL, SUCCESSFUL terminal output of the `python scratch.py` pipeline run (the last 25 lines) in a code block.