[pytest]
asyncio_mode = auto
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    browser: tests requiring browser automation
    live: tests that hit live services
testpaths = tests
